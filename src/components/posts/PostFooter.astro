---
import Footer from "@components/sections/Footer.astro";

interface Props {
  maxWidth: number;
}

const { maxWidth } = Astro.props;
---

<div class="mx-auto my-0 w-10/12" style={`max-width: ${maxWidth}px`}>
  <Footer />
</div>
