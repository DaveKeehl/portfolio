---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div>
  <p class="p4-medium mb-[2px] text-blue-100 opacity-60">{title}</p>
  <p class="p4-semibold text-grayscale-100 whitespace-pre-wrap"><slot /></p>
</div>
